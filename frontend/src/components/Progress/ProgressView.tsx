import { CircularProgress } from "@mui/material";
import "./index.scss";

export default function ProgressView(): JSX.Element {

  return (
    <section id="progress">
      <CircularProgress />
    </section>
  );
}
